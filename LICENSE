→ *New repository*.
- Name it (e.g., `capital-funded-frontend`).
- Choose *Public* or *Private*.
- Don’t initialize with README (we’ll add that locally).
- Click *Create repository*.

---

2. *Prepare your local React project folder:*

- Open your terminal or command prompt.
- Navigate to your project folder:  
  `cd path/to/capital-funded-frontend`

---

3. *Initialize Git and push:*

Run these commands one by one:

```bash
git init
git add .
git commit -m "Initial commit of Capital Funded frontend"
```

---

4. *Connect to your GitHub repo:*

 `<your-username>` and `<repo-name>` accordingly:

```bash
git remote add origin https://github.com/<your-username>/<repo-name>.git
```

---

5. *Push code to GitHub:*

```bash
git branch -M main
git push -u origin main
``
